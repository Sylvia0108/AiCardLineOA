{
  "name": "liff-vite-project",
  "version": "1.0.0",
  "description": "LIFF Vite Project with Express Backend",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start:backend": "cd backend && npm install && npm start",
    "build": "npm run build:frontend",
    "start": "npm run build:frontend && npm run start:backend",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run start:backend\"",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
